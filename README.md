# sudoku

A mobile-friendly, installable Sudoku PWA — vanilla HTML/CSS/JS, no build step, no backend. Puzzle generation, solving, and progress all run client-side, with state saved to `localStorage` so a puzzle resumes right where you left off.
